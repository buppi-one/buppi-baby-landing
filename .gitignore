node_modules
.next
out
*.tsbuildinfo
.env*.local
.DS_Store
.pagespeed
.cloudflare

# generated from content/blog at build time
public/blog
_design-source

# Claude Code — personal/transient (project-scoped skills are committed)
.claude/scheduled_tasks.lock
.claude/settings.local.json
AGENTS.md

# IDE
.idea/

# Service-account key (GA4 + Search Console) — NEVER commit
/credentials/
